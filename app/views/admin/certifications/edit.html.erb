<h1 class="admin-title">Edit Certification</h1>
<%= form_with model: @certification, url: admin_certification_path(@certification.id), method: :patch, local: true, class: "admin-form" do |form| %>
  <%= render "admin/shared/form_fields", form: form, record: @certification, columns: ["name", "issuer", "position"] %>
  <div class="admin-form__actions">
    <%= form.submit "Save", class: "admin-btn admin-btn--primary" %>
    <%= link_to "Cancel", admin_certifications_path, class: "admin-link" %>
  </div>
<% end %>
